{
  "name": "solana-kiss-cli",
  "version": "0.0.1",
  "bin": {
    "solana-kiss": "./dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "prettier": "^3.5.3",
    "prettier-plugin-organize-imports": "^4.3.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "cli-kiss": "0.0.10",
    "solana-kiss": "0.2.18"
  },
  "scripts": {
    "format": "prettier --write src",
    "checks": "tsc --noEmit --skipLibcheck && prettier --check src",
    "build": "tsup"
  }
}
